# Dify4j Docker环境变量配置示例
# 请根据实际情况修改以下配置

# 应用基本配置
APP_NAME=Dify4j
APP_ENV=production
SECRET_KEY=your-secret-key-change-me
INIT_PASSWORD=password

# 数据库配置
DB_HOST=db
DB_PORT=5432
DB_DATABASE=dify
DB_USERNAME=postgres
DB_PASSWORD=difyai123456

# Redis配置
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=difyai123456
REDIS_DB=0

# 向量数据库配置
# 可选值: weaviate, milvus, opensearch, qdrant, pgvector
VECTOR_STORE=weaviate

# Weaviate配置
WEAVIATE_ENDPOINT=http://weaviate:8080
WEAVIATE_API_KEY=WVF5YThaHlkYwhGUSmCRgsX3tD5ngdN8pkih

# Qdrant配置
QDRANT_URL=http://qdrant:6333
QDRANT_API_KEY=difyai123456

# PGVector配置
PGVECTOR_HOST=pgvector
PGVECTOR_PORT=5432
PGVECTOR_USER=postgres
PGVECTOR_PASSWORD=difyai123456
PGVECTOR_DB=dify

# Milvus配置
MILVUS_HOST=milvus-standalone
MILVUS_PORT=19530
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# OpenSearch配置
OPENSEARCH_HOST=opensearch
OPENSEARCH_PORT=9200
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=Qazwsxedc!@#123

# 存储配置
# 可选值: opendal
STORAGE_TYPE=opendal
OPENDAL_SCHEME=fs
OPENDAL_FS_ROOT=./storage

# S3存储配置
# OPENDAL_SCHEME=s3
# OPENDAL_S3_BUCKET=your-bucket
# OPENDAL_S3_ENDPOINT=https://s3.amazonaws.com
# OPENDAL_S3_ACCESS_KEY_ID=your-access-key
# OPENDAL_S3_SECRET_ACCESS_KEY=your-secret-key
# OPENDAL_S3_REGION=us-east-1

# Azure Blob存储配置
# OPENDAL_SCHEME=azblob
# OPENDAL_AZBLOB_ENDPOINT=https://your-account.blob.core.windows.net
# OPENDAL_AZBLOB_CONTAINER=your-container
# OPENDAL_AZBLOB_ACCOUNT_NAME=your-account-name
# OPENDAL_AZBLOB_ACCOUNT_KEY=your-account-key

# 网络配置
NGINX_PORT=80
NGINX_SSL_PORT=443
NGINX_HTTPS_ENABLED=false
DOMAIN_NAME=localhost

# 邮件配置
MAIL_ENABLED=false
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-email-password
MAIL_FROM=your-email@example.com
MAIL_USE_TLS=true
MAIL_USE_SSL=false

# 日志配置
LOG_LEVEL=info

# 应用服务配置
API_PORT=8080
WORKER_PORT=8081
WEB_PORT=3000

# 其他配置
TIMEZONE=Asia/Shanghai
